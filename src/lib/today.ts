import type { SupabaseClient } from "@supabase/supabase-js";
export async function setDailyPriority(client:SupabaseClient,userId:string,taskId:string,position:number){const today=new Date().toISOString().slice(0,10);const owned=await client.from("tasks").select("id").eq("id",taskId).eq("user_id",userId).is("deleted_at",null).maybeSingle();if(!owned.data)throw new Error("Task not found.");const plan=await client.from("daily_plans").upsert({user_id:userId,plan_date:today},{onConflict:"user_id,plan_date"}).select("id").single();if(plan.error)throw plan.error;await client.from("daily_priorities").delete().eq("plan_id",plan.data.id).eq("task_id",taskId);const saved=await client.from("daily_priorities").upsert({user_id:userId,plan_id:plan.data.id,task_id:taskId,position},{onConflict:"plan_id,position"});if(saved.error)throw saved.error;return saved.data}
