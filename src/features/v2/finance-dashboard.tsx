"use client";
import Link from "next/link";
import { useCallback,useState } from "react";
import { Button } from "@/components/ui/button";
import { useDeferredEffect } from "@/lib/use-deferred-effect";
type Row=Record<string,unknown>&{id:string};type Data={currency:string;metrics:Record<string,number>;overdue:Row[];invoices:Row[];subscriptions:Row[]};
const format=(value:number,currency="MAD")=>new Intl.NumberFormat("en",{style:"currency",currency,maximumFractionDigits:0}).format(value);
export function FinanceDashboard(){const[data,setData]=useState<Data|null>(null);const[error,setError]=useState("");const load=useCallback(async()=>{try{const response=await fetch("/api/finance",{cache:"no-store"});const body=await response.json();if(!response.ok)throw new Error(body.error);setData(body)}catch(reason){setError(reason instanceof Error?reason.message:"Finance could not be loaded.")}},[]);useDeferredEffect(useCallback(()=>{void load()},[load]));return <div className="domain-page"><header className="page-header"><div><p className="eyebrow">Business pulse · MAD</p><h1>Finance</h1><p>Cash visibility for decisions—not accounting theatre.</p></div><Link className="button button--solid button--brand" href="/finance/invoices">New invoice</Link></header><nav className="detail-tabs" aria-label="Finance sections">{[["Overview","/finance"],["Invoices","/finance/invoices"],["Expenses","/finance/expenses"],["Subscriptions","/finance/subscriptions"],["Clients","/finance/clients"],["Projects","/finance/projects"]].map(([label,href],index)=><Link className={index===0?"active":""} href={href} key={href}>{label}</Link>)}</nav>{error?<div className="inline-error" role="alert"><p>{error}</p><Button emphasis="outline" onClick={()=>void load()}>Try again</Button></div>:!data?<div className="empty-state"><span>···</span><h2>Calculating cash position</h2></div>:<><section className="metric-ledger" aria-label="Monthly finance summary">{[["Expected this month",data.metrics.expected],["Received this month",data.metrics.received],["Outstanding",data.metrics.outstanding],["Overdue",data.metrics.overdue],["Expenses this month",data.metrics.expenses],["Net cash movement",data.metrics.net]].map(([label,value])=><div key={String(label)}><strong>{format(Number(value),data.currency)}</strong><span>{label}</span></div>)}</section><section className="editorial-split"><div><p className="eyebrow">Needs attention</p><h2>{data.overdue.length?`${data.overdue.length} overdue invoice${data.overdue.length===1?"":"s"}.`:"No overdue invoices."}</h2>{data.overdue.slice(0,5).map((invoice)=><Link className="signal-row" href={`/finance/invoices?invoice=${invoice.id}`} key={invoice.id}><span><strong>{String(invoice.invoice_number??invoice.title??"Invoice")}</strong>{format(Number(invoice.amount_remaining),String(invoice.currency))} · due {String(invoice.due_date)}</span></Link>)}</div><aside><p className="eyebrow">Renewing soon</p>{data.subscriptions.slice(0,5).map((item)=><Link className="signal-row" href="/finance/subscriptions" key={item.id}><span><strong>{String(item.name)}</strong>{format(Number(item.amount),String(item.currency))} · {String(item.next_billing_date??"No renewal date")}</span></Link>)}</aside></section></>}</div>}
