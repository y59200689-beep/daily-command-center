"use client";
import { useCallback,useState } from "react";
import { Button } from "@/components/ui/button";
import { useDeferredEffect } from "@/lib/use-deferred-effect";
type Data={productivity:{completed:number;total:number;completion_rate:number;overdue_rate:number;focus_hours:number;estimate_delta_minutes:number};projects:{active:number};business:{metrics:Record<string,number>;currency:string};content:{items:unknown[];awaitingApproval:unknown[]};fitness:{totals:{sessions:number;distance_km:number;duration_minutes:number}}};
export function AnalyticsDashboard(){const[data,setData]=useState<Data|null>(null);const[error,setError]=useState("");const load=useCallback(async()=>{try{const response=await fetch("/api/analytics",{cache:"no-store"});const body=await response.json();if(!response.ok)throw new Error(body.error);setData(body)}catch(reason){setError(reason instanceof Error?reason.message:"Analytics could not be loaded.")}},[]);useDeferredEffect(useCallback(()=>{void load()},[load]));return <div className="domain-page"><header className="page-header"><div><p className="eyebrow">Four-week signal</p><h1>Analytics</h1><p>Real patterns from work, money, publishing, and movement—without decorative charts.</p></div></header>{error?<div className="inline-error" role="alert"><p>{error}</p><Button emphasis="outline" onClick={()=>void load()}>Try again</Button></div>:!data?<div className="empty-state"><span>···</span><h2>Finding meaningful patterns</h2></div>:<div className="analysis-sections"><Insight title="Productivity" value={`${Math.round(data.productivity.completion_rate*100)}% completion rate`} copy={`${data.productivity.completed} of ${data.productivity.total} recent tasks completed. ${data.productivity.focus_hours.toFixed(1)} focus hours recorded.`}/><Insight title="Projects" value={`${data.projects.active} active projects`} copy="Project attention is calculated from persisted tasks and focus sessions."/><Insight title="Business" value={`${new Intl.NumberFormat("en").format(data.business.metrics.net)} ${data.business.currency} net movement`} copy={`${new Intl.NumberFormat("en").format(data.business.metrics.outstanding)} ${data.business.currency} remains outstanding.`}/><Insight title="Content" value={`${data.content.items.length} active pieces`} copy={`${data.content.awaitingApproval.length} currently need review or approval.`}/><Insight title="Fitness" value={`${data.fitness.totals.sessions} sessions this week`} copy={`${data.fitness.totals.distance_km.toFixed(1)} km and ${Math.round(data.fitness.totals.duration_minutes)} minutes logged.`}/></div>}</div>}
function Insight({title,value,copy}:{title:string;value:string;copy:string}){return <section><p className="eyebrow">{title}</p><h2>{value}</h2><p>{copy}</p></section>}
