"use client";
import Link from "next/link";
import { useCallback, useState } from "react";
import { Button } from "@/components/ui/button";
import { useDeferredEffect } from "@/lib/use-deferred-effect";

type Campaign = { id:string; name:string; channel:string|null; source:string|null; periodStart:string; periodEnd:string; currency:string; spend:number|null; revenue:number|null; orders:number|null; customers:number|null; rows:number; roas:number|null; cac:number|null };
type Data = { campaigns:Campaign[]; comparisons:Record<string,{highestRevenue:string|null;highestRoas:string|null;lowestCac:string|null}|null> };
const amount=(value:number|null,currency:string)=>value==null?"Unavailable":`${value.toLocaleString()} ${currency}`;
export function FounderMarketing(){const[data,setData]=useState<Data|null>(null);const[error,setError]=useState("");const load=useCallback(async()=>{try{const response=await fetch("/api/founder/marketing",{cache:"no-store"});const body=await response.json() as Data&{error?:string};if(!response.ok)throw new Error(body.error??"Campaign performance could not be loaded.");setData(body);setError("")}catch(reason){setError(reason instanceof Error?reason.message:"Campaign performance could not be loaded.")}},[]);useDeferredEffect(useCallback(()=>{void load()},[load]));if(error)return <div className="domain-page"><section className="inline-error" role="alert"><p>{error}</p><Button emphasis="outline" onClick={()=>void load()}>Try again</Button></section></div>;if(!data)return <div className="domain-page"><div className="empty-state"><span>···</span><h2>Loading campaign performance</h2></div></div>;return <div className="domain-page founder-page"><header className="page-header"><div><p className="eyebrow">Founder marketing</p><h1>Campaign performance</h1><p>Based on tracked attribution data. Missing metrics remain unavailable, and currencies are never combined.</p></div></header>{Object.entries(data.comparisons).map(([currency,comparison])=>comparison?<section className="data-surface founder-section" key={currency}><p className="eyebrow">Comparison · {currency}</p><p className="dataset-note">{comparison.highestRevenue?`Highest attributed revenue: ${comparison.highestRevenue}. `:""}{comparison.highestRoas?`Highest ROAS: ${comparison.highestRoas}. `:""}{comparison.lowestCac?`Lowest CAC: ${comparison.lowestCac}.`:""}</p></section>:null)}<section className="founder-grid">{data.campaigns.length?data.campaigns.map(campaign=><article className="data-surface founder-section" key={`${campaign.id}:${campaign.currency}`}><div className="integration-actions"><div><p className="eyebrow">{campaign.channel??"Channel unavailable"}</p><h2>{campaign.name}</h2></div>{!campaign.id.startsWith("unlinked:")?<Link className="button button--ghost" href={`/campaigns/${campaign.id}`}>Open campaign</Link>:null}</div><p className="dataset-note">{campaign.periodStart}–{campaign.periodEnd}{campaign.source?` · ${campaign.source}`:""}</p><div className="metric-ledger metric-ledger--four"><Metric label="Spend" value={amount(campaign.spend,campaign.currency)}/><Metric label="Attributed revenue" value={amount(campaign.revenue,campaign.currency)}/><Metric label="Orders" value={campaign.orders?.toLocaleString()??"Unavailable"}/><Metric label="New customers" value={campaign.customers?.toLocaleString()??"Unavailable"}/><Metric label="ROAS" value={campaign.roas==null?"Unavailable":campaign.roas.toFixed(2)}/><Metric label="CAC" value={amount(campaign.cac,campaign.currency)}/></div>{campaign.rows<3?<p className="dataset-note">Limited tracked sample — comparison labels are withheld.</p>:null}</article>):<section className="data-surface founder-section"><p className="dataset-note">No tracked campaign attribution has been imported.</p></section>}</section></div>}
function Metric({label,value}:{label:string;value:string}){return <div><strong>{value}</strong><span>{label}</span></div>}
