"use client";
import { useCallback, useState } from "react";
import { Button } from "@/components/ui/button";
import { useDeferredEffect } from "@/lib/use-deferred-effect";
type Candidate={id:string;name:string;reason:string;lastCompletedAt:string};
export function ReactivationCandidates({ clientId }: { clientId?: string }) { const [items,setItems]=useState<Candidate[]>([]);const[error,setError]=useState("");const[busy,setBusy]=useState("");const load=useCallback(async()=>{try{const response=await fetch("/api/business/reactivation",{cache:"no-store"});const body=await response.json();if(!response.ok)throw new Error(body.error);setItems((body.candidates as Candidate[]).filter((item)=>!clientId||item.id===clientId));setError("")}catch(reason){setError(reason instanceof Error?reason.message:"Reactivation candidates could not be loaded.")}},[clientId]);useDeferredEffect(useCallback(()=>{void load()},[load]));const act=async(id:string,action:"create_opportunity"|"dismiss"|"snooze")=>{setBusy(`${id}:${action}`);try{const response=await fetch("/api/business/reactivation",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({clientId:id,action})});const body=await response.json();if(!response.ok)throw new Error(body.error);setItems(current=>current.filter(item=>item.id!==id));}catch(reason){setError(reason instanceof Error?reason.message:"This reactivation action could not be saved.")}finally{setBusy("")}};return <section className="data-surface reactivation-candidates"><p className="eyebrow">Potential reactivation</p>{error?<p className="field-error">{error}</p>:null}{items.length?items.map(item=><article className="signal-row" key={item.id}><span><strong>{item.name}</strong>{item.reason}<small>Last completed work {new Date(item.lastCompletedAt).toLocaleDateString()}.</small></span><div className="integration-actions"><Button disabled={Boolean(busy)} onClick={()=>void act(item.id,"create_opportunity")}>{busy===`${item.id}:create_opportunity`?"Creating…":"Create opportunity"}</Button><Button emphasis="ghost" disabled={Boolean(busy)} onClick={()=>void act(item.id,"snooze")}>Snooze</Button><Button emphasis="ghost" disabled={Boolean(busy)} onClick={()=>void act(item.id,"dismiss")}>Dismiss</Button></div></article>):<p className="dataset-note">No past client currently needs a reactivation prompt.</p>}</section> }
