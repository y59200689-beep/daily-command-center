import { ResourceWorkspace } from "@/features/founder-os/resource-workspace";
export const metadata = { title: "KPI observations" };
export default function Page() { return <ResourceWorkspace resource="kpi-observations" />; }
