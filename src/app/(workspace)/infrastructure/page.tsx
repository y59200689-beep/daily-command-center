import { ResourceWorkspace } from "@/features/founder-os/resource-workspace";
export const metadata = { title: "Infrastructure" };
export default function Page() { return <ResourceWorkspace resource="infrastructure" />; }
