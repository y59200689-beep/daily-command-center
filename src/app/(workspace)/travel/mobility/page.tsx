import { ResourceWorkspace } from "@/features/founder-os/resource-workspace";
export const metadata = { title: "Mobility" };
export default function Page() { return <ResourceWorkspace resource="mobility" />; }
