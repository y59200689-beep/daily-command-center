import { ResourceWorkspace } from "@/features/founder-os/resource-workspace";
export const metadata = { title: "Training plans" };
export default function Page() { return <ResourceWorkspace resource="training-plans" />; }
