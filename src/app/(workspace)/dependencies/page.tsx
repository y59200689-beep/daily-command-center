import { ResourceWorkspace } from "@/features/founder-os/resource-workspace";
export const metadata = { title: "Dependencies" };
export default function Page() { return <ResourceWorkspace resource="dependencies" />; }
