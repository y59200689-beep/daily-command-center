import type { Metadata } from "next";import { FinanceCollection } from "@/features/v2/finance-collection";export const metadata:Metadata={title:"Subscriptions — Daily Command Center"};export default function Page(){return <FinanceCollection domain="subscriptions"/>}
