import { ResourceWorkspace } from "@/features/founder-os/resource-workspace";
export const metadata = { title: "Critical issues" };
export default function Page() { return <ResourceWorkspace resource="issues" />; }
