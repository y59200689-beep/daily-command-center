import { CommerceReviewView } from "@/features/commerce/commerce-review-view";

export const metadata = {
  title: "Commerce Review | Daily Command Center",
  description: "Operating review records, stock turnover governance, and action items.",
};

export default function CommerceReviewPage() {
  return <CommerceReviewView />;
}
