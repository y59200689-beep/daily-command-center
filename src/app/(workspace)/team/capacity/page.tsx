import { CapacityView } from "@/features/team/capacity-view";

export const metadata = {
  title: "Capacity & Workload · Team OS",
  description: "Objective workload balance and commitment capacity across team",
};

export default function CapacityPage() {
  return <CapacityView />;
}
