import { RetentionReviewView } from "@/features/success/retention-review-view";

export const metadata = {
  title: "Retention Review | Daily Command Center",
  description: "Weekly and monthly retention review with risks, issues, and renewal pipeline.",
};

export default function RetentionReviewPage() {
  return <RetentionReviewView />;
}
