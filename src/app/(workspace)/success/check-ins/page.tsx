import { CheckInsView } from "@/features/success/check-ins-view";

export const metadata = {
  title: "Client Check-Ins | Daily Command Center",
  description: "Track and manage client touchpoints, reviews, and check-in sessions.",
};

export default function CheckInsPage() {
  return <CheckInsView />;
}
