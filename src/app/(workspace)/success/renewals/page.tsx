import { RenewalsView } from "@/features/success/renewals-view";

export const metadata = {
  title: "Renewals | Daily Command Center",
  description: "Track upcoming client renewals, forecast categories, and preparation state.",
};

export default function RenewalsPage() {
  return <RenewalsView />;
}
