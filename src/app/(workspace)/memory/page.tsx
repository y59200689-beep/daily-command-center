import type { Metadata } from "next";
import { MemoryPage } from "@/features/intelligence/intelligence-pages";
export const metadata: Metadata = { title: "Memory — Daily Command Center" };
export default function Page() { return <MemoryPage/>; }
